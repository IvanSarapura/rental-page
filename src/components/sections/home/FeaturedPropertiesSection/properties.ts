// Re-export from the centralized data layer.
// Keep this file so existing relative imports within the section folder still resolve.
export type { Property } from "@/data/properties";
export { featuredProperties } from "@/data/properties";
